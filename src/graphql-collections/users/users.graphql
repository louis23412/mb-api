type Query {
    viewUserProfile(
        username: String!
    ) : UserProfile!
}

type UserProfile {
    username: String!
    lvl: Float!
    currencies: Currencies!
}

type Currencies {
    credits: Int!
    darkMatter: Int!
}